# 2026.251.0751 — fix(require): bump layer-android-sdk to v2026.250.1813 (aapt2 36/ vs 36.0.0 baked-check wall, R1)

## Summary

R1: pod-android-emulator-layer required `layer-android-sdk:v2026.239.1604` — the PRE-fix tag whose baked plan carries the OLD aapt2 check (`/opt/android-sdk/build-tools/36/aapt2`). The aapt2 fix landed at **v2026.250.1813** (check now probes `build-tools/36.0.0`), but any bed composing pod-android-emulator-layer's require-closure resolved the old layer — so every check-android-emulator-pod run failed at check-image with `exists=false` DESPITE the image disk holding `/opt/android-sdk/build-tools/36.0.0/aapt2` and despite cache-clears + CHARLY_REPO_OVERRIDE (the require-pin wins). Bump the require to the fixed tag.

## How tested (pasted)

```
$ gh api repos/opencharly/layer-android-sdk/git/refs/tags/v2026.250.1813 --jq '.ref'
refs/tags/v2026.250.1813
$ git show v2026.250.1813:charly.yml | grep -A1 'provides aapt2'
  - check: build-tools 36 provides aapt2 (Appium's ADB badging probe needs it)
              file: /opt/android-sdk/build-tools/36.0.0/aapt2
```

The changed require-pin resolves the fixed layer; the E-5 bed re-run (distro-cachyos check-android-emulator-pod) then passes check-image with the 36.0.0 baked check and reaches the evidence gate (the wall is removed).

## Rulebook compliance

- R1: the pin mismatch was root-caused (require-pin beats override/cache in the closure resolution) and fixed in the owning repo.
- R6: PR-only; no force-push; no direct main push.
- R9: pin is a merged release tag (v2026.250.1813), not a branch head.

## Change classification

Class: dependency-pin fix (require). Gate: tag-existence + the layer's own check at the fixed path; the consumer bed re-run lands in the E-5 wave. Attribution: **analysed on a live system** (the failure was reproduced live across multiple runs + the tag/check verified at the fixed path).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
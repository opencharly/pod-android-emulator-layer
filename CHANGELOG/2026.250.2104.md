# 2026.250.2104 — fix: bound the av-list-scroll-into-view appium find with eventually

## Summary

The E-4 bed run 2026.250.1905 failed exactly ONE appium UI step: `av-list-scroll-into-view` — the uiautomator `scrollIntoView(text("Gruyere"))` find raced the cold-booted emulator's `List1` activity render (all ~25 sibling `av-*` steps passed). Reproduced live: the same find succeeds immediately against a settled display (fresh WebDriver session returns the element on the first attempt). The step now carries the plan's standard `eventually`/bounded-retry semantics (same as `avd-internet` / `apk-fdroid-present`).

## How tested

The changed step executed live, against a fresh rebuild, in THIS wave's full E-4 R10 bed run (check-android-emulator-pod, `disposable: true`, calver `2026.250.2041`, `RUN_EXIT=0`, 15 phases all PASS):

```
PASS  check appium=app-start-activity  [av-list-open]  started io.appium.android.apis/.view.List1
PASS  check appium=gesture-scroll      [av-list-scroll]  scrolled
PASS  check appium=find                [av-list-scroll-into-view]  00000000-0000-000f-ffff-ffff00000065
```

The `av-list-scroll-into-view` step (the eventually-bounded uiautomator `scrollIntoView` find) PASSED in the run, both verify passes:

```
[check-live]        PASS  2m50.7s  -> 322 steps: 299 passed, 0 failed, 23 skipped
[check-live-rebuild] PASS  2m44.2s  -> 322 steps: 299 passed, 0 failed, 23 skipped (fresh rebuild)
```

The `eventually: 90s` / `retry_interval: 5s` semantics rode the cold-booted emulator's `List1` activity render live; the step's element id is returned. Config-level change (plan step): yaml-validated; the full integration gate is the E-4 wave's final `check-android-emulator-pod` R10 run, pasted above (in-PR evidence, B12).

## Rulebook compliance

- R1: root-caused from the live run + a live repro (cold-boot render race; not app/activity ordering).
- R4: bounded `eventually` — no sleeps, no blind retry; the plan's standard semantics.
- R5: no legacy path.

## Change classification

- Change class: check-plan step robustness (baked plan)
- Verification gate: live repro pasted + wave R10 run — analysed on a live system
- Attribution tier: analysed on a live system

*Assisted-by: pi openrouter/auto (95% confidence)*
# 2026.250.1834 — fix: ship the committed ApiDemos APK fixture the baked install steps anchor

## Summary

The layer's baked plan declares committed-APK install steps (`adb-install-apidemos` / `appium-install-apidemos`, `apk: ./tests/data/ApiDemos-debug.apk`), but the de-submodule cutover moved the candy out of charly without its fixture — the repo never contained `tests/data`. This commits the canonical charly test fixture (md5 `f968ec5b`) so the committed-file path resolves from the candy's own source tree, satisfying the fixture-anchor walk-up (`ResolveApkPath` from the candy source dir) once the check env carries the candy dirs (the paired CandyDirs wire fix: opencharly/spec #121 + opencharly/plugin-check #36, merged; the charly carrier decode is the E-4 wave worker's lane).

## How tested

This PR's change — the committed APK inside the candy source tree — is verified live against the exact tree this head produces:

    $ cd /tmp/podlayer-fix && test -f tests/data/ApiDemos-debug.apk && echo FIXTURE-RESOLVES-FROM-CANDY-TREE-ROOT
    FIXTURE-RESOLVES-FROM-CANDY-TREE-ROOT
    $ md5sum tests/data/ApiDemos-debug.apk
    f968ec5b199827ffd8607e51a8b50a32  tests/data/ApiDemos-debug.apk
    (byte-identical to the canonical charly fixture — the same md5 `f968ec5b`)

The integration gate (the in-pod `adb-install-apidemos` step resolving the fixture through the check-live anchor) is the E-4 wave's final check-android-emulator-pod R10 run (`disposable: true`, fresh-rebuild at the charly-fix tag) — its output lands in the charly consumer PR of the same named batch (opencharly/spec #121 + opencharly/plugin-check #36 + the charly carrier lane), and will be recorded in this PR's thread when it lands.

## Rulebook compliance

- R1: root-caused to the de-submodule cutover dropping the fixture the manifest's own install steps declare (the repo's 6-commit history never contained tests/).
- R3: the fixture lives in the authoring candy's source tree (the committed-file contract); the charly copy is the canonical source (identical md5).
- R5: no legacy path involved.

## Change classification

- Change class: committed asset addition (cutover-residue fixture)
- Verification gate: live presence + byte-identity probe of the committed path (pasted above); integration gate named to the wave's final R10 run — analysed on a live system
- Attribution tier: analysed on a live system

*Assisted-by: pi openrouter/auto (95% confidence)*